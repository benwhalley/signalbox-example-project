Site specific settings and 'front end' apps
============================================

For each installation, Signalbox requires that a 'front-end' django app be installed to hold all the assets (like images, stylesheets) needed to create a public website for the trial.

Within this app, you must include a ``settings.py`` file, which can contain the following settings:


.. automodule:: app.reframed.settings


